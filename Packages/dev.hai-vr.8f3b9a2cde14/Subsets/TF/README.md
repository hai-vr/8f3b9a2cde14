TF
===

The objective of TF is to provide a trigger-like component.

This trigger-like component is converted to C#, to be run natively.
- This enables the complete removal of TF from the project.
- This enables the possible use of `[Cilboxable]` on the generated classes, for use with UGC.
